# Week 6A - DropdownButton and TextField Widgets

Note for Fall 2025; this would have normally been covered class 5B. However we slowed things down a bit to review for project 1.
Additionally this allows the entire week 6 to be focused on forms / user inputs which may make more sense in the end.

## What We Covered in Class

### DropdownButton Widget
We started with dropdown menus - a common UI element for giving users a predefined list of options. Unlike web development, Flutter's dropdowns require more setup but offer more control.

**Key Points:**
- DropdownButton requires a list of DropdownMenuItem widgets
- Each item needs both a `value` (for your code) and `child` (what users see)
- The selected value must match one of the item values exactly
- **Critical:** Always wrap `onChanged` in `setState()` or the UI won't update

**Two Approaches We Learned:**

1. **Explicit DropdownMenuItems** (more control):
```dart
var armorList = [
  DropdownMenuItem(value: 'leather', child: Text('Leather Armor')),
  DropdownMenuItem(value: 'chain', child: Text('Chain Mail')),
  // ... more items
];
```

2. **Using .map() with string lists** (cleaner for simple cases):

--- Flashback to IGME-330. Remember we used Map to append list items to a list in html? We may have combined this with AJAX. Similar idea; a clean alternative to long for loops. It takes a while to get used to the syntax; but with some practice it should start to make sense. Even if you struggle to write these from scratch you want to have a good understanding of what it is doing. This is a common practice you will see if you get into full stack development and are using popular frameworks such as React.

```dart
List<String> weaponList = ['dagger', 'sword', 'mace', 'spear'];

// In the DropdownButton:
items: weaponList.map((String item) {
  return DropdownMenuItem(value: item, child: Text(item));
}).toList(),  // Don't forget .toList()!
```

### TextField Widget - The Basics
TextFields are deceptively complex in Flutter. A simple `TextField()` gives you basic input, but real apps need much more.

**What We Did:**
- Created basic text input fields
- Captured input using `onSubmitted` (triggered by keyboard checkmark)
- Styled fields with InputDecoration
- Implemented different keyboard types for better UX

### InputDecoration - Making TextFields Beautiful
The `decoration` property transforms plain TextFields into professional-looking inputs:

```dart
TextField(
  decoration: InputDecoration(
    label: Text('Enter your name'),
    hintText: 'John Doe',
    border: OutlineInputBorder(),
    contentPadding: EdgeInsets.all(20),
    filled: true,
    fillColor: Colors.blue[50],
    prefixIcon: Icon(Icons.person),
    suffixIcon: Icon(Icons.clear),
  ),
)
```

**Remember:** 
- `filled` must be `true` for `fillColor` to work
- `enabledBorder` styles the field when not focused
- `focusedBorder` styles the field when user is typing

### Keyboard Types & Navigation
We explored how to show appropriate keyboards for different data types:

```dart
// Numeric input only
TextField(
  keyboardType: TextInputType.number,
)

// Date/time input
TextField(
  keyboardType: TextInputType.datetime,
)

// Email with @ symbol prominent
TextField(
  keyboardType: TextInputType.emailAddress,
)
```

**Text Input Actions** for better form flow:
- `TextInputAction.next` - Shows arrow to go to next field
- `TextInputAction.previous` - Shows back arrow
- `TextInputAction.done` - Shows checkmark to complete

### TextEditingController - Power User Features
Controllers give you programmatic control over TextFields:

```dart
// Create controller
final txtNameController = TextEditingController();

// Use in TextField
TextField(
  controller: txtNameController,
)

// Pre-fill text
@override
void initState() {
  super.initState();
  txtNameController.text = 'Default text';
}

// Clean up to prevent memory leaks
@override
void dispose() {
  txtNameController.dispose();
  super.dispose();
}

// Clear programmatically
IconButton(
  icon: Icon(Icons.clear),
  onPressed: () => txtNameController.clear(),
)
```

**Why Use Controllers?**
- Pre-fill fields with data
- Clear fields with a button tap
- Access text value anytime (not just on submit)
- Add listeners for real-time updates
- Required for form validation (coming next class)

## Key Concepts & Terminology

| Term | Definition |
|------|------------|
| DropdownMenuItem | Individual option in a dropdown list |
| onChanged | Callback fired when dropdown selection changes |
| InputDecoration | Styling configuration for TextField |
| TextInputType | Determines which keyboard appears |
| TextInputAction | Controls keyboard action button behavior |
| TextEditingController | Manages TextField state programmatically |
| initState | Lifecycle method that runs before widget builds |
| dispose | Lifecycle method for cleanup when widget is removed |

## What You Should Do Before Next Class

### Required:
1. **Practice TextField Styling**
   - Create a login form with username and password fields
   - Style them to look professional
   - Add appropriate icons and hints

2. **Start Project 2 Planning**
   - Choose your API (Pokémon, Anime, etc.)
   - Think about what search fields you'll need
   - Consider what dropdown filters might be useful

### Optional but Recommended:
1. **Explore More Keyboard Types**
   - Try `TextInputType.phone`
   - Test `TextInputType.multiline` for larger text areas
   - See how `TextInputType.url` differs

2. **Controller Practice**
   - Create a form with 3 fields and controllers
   - Add a "Clear All" button that clears all fields
   - Pre-populate fields with saved data

## Common Issues & Solutions

| Problem | Solution |
|---------|----------|
| Dropdown UI doesn't update | Wrap `onChanged` callback in `setState()` |
| "String? can't be assigned to String" | Add question mark: `String?` for nullable dropdown values |
| Keyboard covers TextField | Wrap page in SingleChildScrollView |
| fillColor doesn't show | Set `filled: true` in InputDecoration |
| Controller text not updating UI | Use `addListener` with `setState` or just use `onChanged` |
| Memory leak warnings | Always dispose controllers in `dispose()` method |
| Can't clear TextField | Use a TextEditingController and call `.clear()` |

## Code/Commands from Class

```dart
// Complete DropdownButton with setState
String? selectedValue = 'option1';

DropdownButton<String>(
  value: selectedValue,
  items: myList.map((String item) {
    return DropdownMenuItem(
      value: item,
      child: Text(item),
    );
  }).toList(),
  onChanged: (String? newValue) {
    setState(() {
      selectedValue = newValue;
    });
  },
)

// TextField with clear button
final myController = TextEditingController();

TextField(
  controller: myController,
  decoration: InputDecoration(
    suffixIcon: IconButton(
      icon: Icon(Icons.clear),
      onPressed: () => myController.clear(),
    ),
  ),
)

// Lifecycle methods for controllers
@override
void initState() {
  super.initState();
  myController.text = 'Initial value';
}

@override
void dispose() {
  myController.dispose();
  super.dispose();
}
```

## Project 2 Connection

Today's material is **essential** for Project 2:
- **TextField**: For search queries to your API
- **DropdownButton**: For filtering results (categories, types, etc.)
- **Controllers**: For managing search state
- **Keyboard types**: For appropriate input (numbers for IDs, text for names)

Start thinking about your Project 2 search interface now!

## Key Takeaways

1. **Always use setState** - Both DropdownButton and TextField need setState to update UI
2. **Controllers are powerful** - More setup but much more capability than simple strings
3. **Decoration matters** - Good InputDecoration makes fields intuitive and professional
4. **Match keyboards to data** - Use appropriate TextInputType for better UX
5. **Dispose controllers** - Prevent memory leaks by cleaning up in dispose()

## Resources & Links

- [Flutter TextField Documentation](https://api.flutter.dev/flutter/material/TextField-class.html)
- [InputDecoration Properties](https://api.flutter.dev/flutter/material/InputDecoration-class.html)
- [DropdownButton Documentation](https://api.flutter.dev/flutter/material/DropdownButton-class.html)
- [TextEditingController Guide](https://api.flutter.dev/flutter/widgets/TextEditingController-class.html)

## Looking Ahead

Next class (Week 6B) we'll cover:
- Form widget for validation
- CheckBox and CheckboxListTile
- ListView for scrollable lists
- Preparing for asynchronous programming

## Need Help?

- TextField styling is complex - experiment with different properties
- Project 2 will need search functionality - start planning now
- Office hours available for Project 2 API selection help
- Remember: `flutter clean` before submitting any project!

---

*Week 6A - IGME-340 - Multi-platform Media App Development*
