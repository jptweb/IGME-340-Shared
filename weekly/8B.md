# Week 8B - Advanced TextFields & Responsive Design

> **📖 Quick Reference:** See the [TextField Basics Reference](../reference/input-forms/textfield-basics.md) and [Focus Management Reference](../reference/input-forms/focus-management.md) for quick code examples and patterns.

## What We Covered in Class

### Part 1: Responsive Layouts with Expanded Widget

> **📖 Quick Reference:** See the [Expanded & Flexible Reference](../reference/widgets/expanded-flexible.md) for responsive layout patterns and best practices.

We started by revisiting the **Expanded widget** - which we briefly touched on before, but now explored in depth for creating truly responsive layouts.

**The Problem We Solved:**

When building layouts with Rows, you often need to guess the width of containers. This causes issues:
- Layouts break when rotating the device (portrait → landscape)
- Apps look different (bad) on tablets vs phones
- Hard-coded widths = unprofessional results

**The Expanded Solution:**

The `Expanded` widget automatically fills remaining space in a Row or Column without you having to calculate or guess dimensions.

#### Key Code Pattern:
```dart
Row(
  children: [
    Container(width: 100, height: 100, /* fixed size */),
    SizedBox(width: 20),
    Expanded(
      child: Container(
        height: 100,  // Only height specified!
        child: Text('This adapts to available width'),
      ),
    ),
  ],
)
```

**What Happens:**
- Expanded calculates: `remaining width = screen width - 100 - 20`
- Container fills exactly the right amount of space
- Rotate device? It recalculates automatically

#### Expanded Works Vertically Too!

In a `Column`, Expanded fills remaining **vertical** space:

```dart
Column(
  children: [
    Container(height: 100, /* top fixed content */),
    Expanded(
      child: Container(
        color: Colors.amber,
        child: Text('Fills remaining screen height'),
      ),
    ),
  ],
)
```

**Use Cases:**
- Main content area that adapts to different screen sizes
- Scrollable sections that fill available space
- Responsive sidebars or navigation panels

---

### Part 2: Professional TextField UX Patterns

Building on our Week 6A TextField knowledge, we added professional user experience patterns that real apps need.

#### Triple-Quote Text Blocks

Before diving into TextFields, we learned a handy Dart feature:

```dart
String bulkText = """
  This is a multi-line string.
  
  It preserves formatting including paragraph breaks.
  
  Great for placeholder text or multi-paragraph content.
""";
```

**Why it matters:** Cleaner code when you need lorem ipsum or multi-line labels.

---

### IMPORTANT NOTICE:
When using the latest android emulator it MAY not show the on screen keyboard anymore! When you click a text field if you aren't seeing the on screen key
board slide out from below and are instead seeing a little circle with 3 lines appearing you aren't getting the true expereince that someone would
get on a mobile device. Since we want to simulate what an end user will see and be sure it works nicely for them we need to change some settings
to get the slide out on screen keyboard to work (like it does on a physical android device.

I'd strongly suggest you follow the steps here: https://github.com/jptweb/IGME-340-Shared/blob/main/weekly/6A.md#android-emulator-keyboard-note which will
walk you through changing the android emulator settings. When I grade your projects in android I will want to see that you designed your fields to not
be covered up by the onscreen keyboard! So please make sure to follow those steps! Otherwise the next few steps are impossible to test / simulate.

### Part 3: Adding Clear Buttons to TextFields

We added clear buttons (the "X" icon) to let users quickly erase field content:

```dart
TextField(
  controller: nameController,
  decoration: InputDecoration(
    labelText: 'Name',
    suffixIcon: IconButton(
      onPressed: () {
        nameController.clear();  // Wipes the field
      },
      icon: Icon(Icons.clear),
    ),
  ),
)
```

**Critical:** You MUST use a `TextEditingController` for this to work.

#### The Problem This Created

Adding the clear button broke our keyboard navigation! When you pressed "next" on the keyboard, it jumped to the clear button instead of the next TextField.

**Why?** The IconButton became part of the focus order - it's now a focusable widget in the tab sequence.

---

### Part 4: Focus Nodes - Taking Control of Navigation

> **📖 Quick Reference:** See the [Focus Management Reference](../reference/input-forms/focus-management.md) for complete focus control patterns and keyboard dismissal techniques.

To fix the broken navigation, we learned about **Focus Nodes** - objects that let you programmatically control which field has focus.

#### Step 1: Create Focus Nodes

```dart
class _Week8bState extends State<Week8b> {
  // Define them at the top
  late FocusNode nameFocusNode;
  // Repeat for additional form fields here!
  
  @override
  void initState() {
    super.initState();
    // Initialize them
    nameFocusNode = FocusNode();
    //Repeat for additional form field!
  }
  
  @override
  void dispose() {
    // CRITICAL: Clean up to prevent memory leaks
    nameFocusNode.dispose();
    //Repeat for additional form field!

    // Also dispose controllers
    nameController.dispose();
    //Repeat for additional form field!
    super.dispose();
  }
}
```

⚠️ **Critical:** Always use `late` keyword and initialize in `initState()`, then dispose in `dispose()`.

#### Step 2: Assign Focus Nodes and Control Navigation

```dart
TextField(
  controller: nameController,
  focusNode: nameFocusNode,  // Assign the node
  decoration: InputDecoration(
    labelText: 'Name',
    suffixIcon: IconButton(
      onPressed: () => nameController.clear(),
      icon: Icon(Icons.clear),
    ),
  ),
  textInputAction: TextInputAction.next,
  onEditingComplete: () {
    emailFocusNode.requestFocus();  // Jump to email field
  },
)
```

**What Changed:**
- `focusNode` property assigns the Focus Node
- `onEditingComplete` fires when user presses "next" on keyboard
- `requestFocus()` manually moves focus to the next field

**For the Last Field:**

```dart
TextField(
  controller: pwdFocusNode,
  focusNode: pwdFocusNode,
  decoration: InputDecoration(labelText: 'Password'),
  textInputAction: TextInputAction.done,
  onEditingComplete: () {
    pwdFocusNode.unfocus();  // Dismiss keyboard
  },
)
```

---

### Part 5: Dismissing the Keyboard

We learned THREE ways to dismiss the keyboard:

#### Method 1: Focus Manager (On Button Press)

```dart
ElevatedButton(
  onPressed: () {
    FocusManager.instance.primaryFocus?.unfocus();
    // Process form submission...
  },
  child: Text('Submit'),
)
```

**When to use:** On submit buttons or any action that should hide the keyboard.

#### Method 2: onTapOutside (Per-Field Basis)

```dart
TextField(
  controller: nameController,
  focusNode: nameFocusNode,
  decoration: InputDecoration(labelText: 'Name'),
  onTapOutside: (event) {
    nameFocusNode.unfocus();
  },
)
```

**When to use:** If you want each field to handle its own tap-outside behavior.
**Downside:** Must add to every field.

#### Method 3: GestureDetector (App-Wide Solution)

```dart
@override
Widget build(BuildContext context) {
  return GestureDetector(
    onTap: () {
      FocusManager.instance.primaryFocus?.unfocus();
    },
    child: Scaffold(
      appBar: AppBar(/* ... */),
      body: /* ... */,
    ),
  );
}
```

**When to use:** Best practice for forms - handles tap-anywhere-to-dismiss.
**Result:** Tapping anywhere outside the keyboard dismisses it (standard mobile UX).

---

### Part 6: Preventing Keyboard from Covering Fields

When the keyboard appears, it can cover bottom fields. We fixed this with `SingleChildScrollView`:

```dart
body: SingleChildScrollView(  // Wrap your entire body
  child: Container(
    child: Column(
      children: [
        // All your layout widgets...
      ],
    ),
  ),
)
```
> Note: this gets a little wonky and sometimes seems to crash my emulator. Adding the singleScrollListener seems to work but occasionally the
> app will "pasue" for debugging, and appear to lock up. Be sure to look at the debug logs while doing this, and make sure the app isn't set to pause
> in the Debugging options (the pause / play button you see at top of VS code when running an app when debugging).

**What it does:** When keyboard appears, the screen automatically scrolls to show the active field.

---

### Part 7: Icon Placement Options (Quick Demo)

We clarified the difference between icon properties in `InputDecoration`:

| Property | Location | When Visible |
|----------|----------|--------------|
| `icon` | Outside field border (left side) | Always |
| `prefixIcon` | Inside field border (left side) | Always |
| `prefix` | Inside field border (left side) | Only when field is focused |

```dart
// Icon OUTSIDE the border
TextField(
  decoration: InputDecoration(
    labelText: 'Name',
    icon: Icon(Icons.person),  // Outside on left
    border: OutlineInputBorder(),
  ),
)

// prefixIcon INSIDE the border
TextField(
  decoration: InputDecoration(
    labelText: 'Email',
    prefixIcon: Icon(Icons.email),  // Inside on left
    border: OutlineInputBorder(),
  ),
)
```

---

## Key Concepts & Terminology

| Term | Definition |
|------|------------|
| **Expanded** | Widget that fills remaining space in Row/Column |
| **FocusNode** | Object that represents keyboard focus for a specific widget |
| **FocusManager** | Global controller for managing focus across the app |
| **onEditingComplete** | Callback fired when user presses action button on keyboard |
| **onTapOutside** | Callback fired when user taps outside a TextField |
| **requestFocus()** | Method to programmatically move focus to a field |
| **unfocus()** | Method to remove focus and dismiss keyboard |
| **SingleChildScrollView** | Widget that makes content scrollable (prevents keyboard overlap) |

---

## What You Should Do Before Next Class

### Required:

**Practice Focus Management:**
1. Create a 4-field form (name, email, phone, message)
2. Add controllers and focus nodes for all fields
3. Chain navigation: name → email → phone → message → dismiss keyboard
4. Add clear buttons that don't break navigation
5. Wrap in GestureDetector for tap-to-dismiss

**Practice Responsive Layouts:**
1. Build a Row with image-text-image layout
2. Use Expanded for the text section
3. Test in both portrait and landscape orientations
4. Add a Column with Expanded to fill vertical space

### Optional but Recommended:

**Combine Both Concepts:**
- Build a responsive form that adapts to orientation
- Use Expanded to manage form layout
- Add professional focus management
- Test on different screen sizes in the emulator

---

## Common Issues & Solutions

| Problem | Solution |
|---------|----------|
| "Next" goes to clear button instead of next field | Add FocusNode with `onEditingComplete` |
| Keyboard covers bottom fields | Wrap body in `SingleChildScrollView` |
| Focus Nodes cause crashes | Always initialize in `initState()` and dispose in `dispose()` |
| Can't dismiss keyboard with tap | Wrap Scaffold in GestureDetector with FocusManager.unfocus() |
| Expanded doesn't work | Make sure it's a direct child of Row/Column |
| Layout breaks on rotation | Replace hard-coded widths with Expanded |

---

## Key Takeaways

1. **Expanded = Responsive** - Stop guessing widths, let Expanded calculate them
2. **Focus Nodes = Control** - Take charge of navigation flow in forms
3. **Always Dispose** - Focus Nodes and Controllers must be cleaned up
4. **Three Dismissal Methods** - Choose based on your needs (button, per-field, or app-wide)
5. **SingleChildScrollView** - Prevents keyboard from covering fields
6. **Test Rotations** - Always test landscape mode to verify responsive behavior

---

## Project Relevance

**For Project 2:**
- Use Expanded for responsive search result layouts
- Implement focus management for search forms
- Add professional keyboard handling
- Ensure your app works in both orientations

**For Project 3:**
- These patterns are essential for any app with forms
- Focus control improves user experience significantly
- Responsive layouts are expected in professional apps

---

## Resources & Links

- [Focus and text fields - Flutter Documentation](https://docs.flutter.dev/cookbook/forms/focus)
- [TextField Class Documentation](https://api.flutter.dev/flutter/material/TextField-class.html)
- [Expanded Widget Documentation](https://api.flutter.dev/flutter/widgets/Expanded-class.html)
- [GestureDetector Documentation](https://api.flutter.dev/flutter/widgets/GestureDetector-class.html)

---

## Looking Ahead

Next class (Week 9A) we'll cover:
- Project 2 introduction and requirements
- Data persistence with `shared_preferences`
- Opening external apps with `url_launcher`
- Caching images with `cached_network_image`

---

## Need Help?

- Focus management can be tricky - test incrementally
- Draw out your focus flow on paper before coding
- Use print statements to debug focus changes: `print('Focus moved to email field')`
- Remember: `flutter clean` before submitting projects!

---

*Week 8B - IGME-340 - Multi-platform Media App Development*
